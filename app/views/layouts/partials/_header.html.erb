<header>
  <div class='flex flex-row justify-between w-full'>
    <h1>Ola, <%= current_user.username.capitalize %></h1>
    <%= link_to "Sair", destroy_user_session_path, data: {turbo_method: :delete}, class: "hover:underline" %>
  </div>
  <div class="divider"></div> 
</header>